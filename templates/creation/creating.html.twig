{% extends 'base.html.twig' %}
{% block title %}Spell Creating{% endblock %}
    {% block body %}
        {% if app.session.get('user') %}
            {{form_start(form)}}
            {{form_widget(form)}}
            {{form_end(form)}}
            {% else %}
            {{ render("/login") }}
        {% endif %}
    {% endblock %}
    
